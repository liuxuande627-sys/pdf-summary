# KWZero MVP - 关键词发现工具

一个现代化的关键词研究工具，专为小型企业和创业者设计。

## 功能特点

- 🎯 **3次免费试用** - 体验完整功能
- 💳 **30次付费生成** - $4.90一次性付费
- 🍎 **苹果风格设计** - 现代化、优雅的界面
- 📊 **真实数据驱动** - 基于Google Trends和Keyword Planner数据
- 🔍 **8大行业覆盖** - SaaS、电商、B2B服务等
- 📱 **完全响应式** - 适配所有设备

## 技术架构

- **后端**: Node.js + Express
- **前端**: 原生HTML/CSS/JavaScript (Tailwind CSS)
- **数据**: JSON文件存储（零成本）
- **支付**: Stripe Checkout集成
- **部署**: Vercel一键部署

## 快速开始

### 本地开发

1. **安装依赖**
   ```bash
   npm install
   ```

2. **配置环境变量**
   ```bash
   cp .env.mvp .env
   # 编辑.env文件，添加你的Stripe密钥
   ```

3. **启动服务器**
   ```bash
   npm run dev
   ```

4. **访问应用**
   - 网站: http://localhost:3000
   - API测试: http://localhost:3001/api/health

### 部署到Vercel

1. **Fork此仓库**
2. **在Vercel导入项目**
3. **设置环境变量**:
   - `STRIPE_SECRET_KEY`
   - `STRIPE_WEBHOOK_SECRET`
   - `STRIPE_PUBLISHABLE_KEY`
4. **部署**

## API文档

### 健康检查
```
GET /api/health
```

### 获取行业列表
```
GET /api/industries
```

### 生成关键词
```
POST /api/keywords
{
  "industry": "SaaS"
}
```

### 创建支付会话
```
POST /api/payment/create-checkout
```

## 关键词算法

使用以下公式计算关键词评分：
```
score = (volume × 0.7 + cpc × 100) / (difficulty + 1)
```

筛选条件：
- 搜索量 > 100
- 难度 < 30
- 按评分降序排列

## 项目结构

```
├── server-mvp.js          # Express服务器
├── public/               # 静态文件
│   ├── index.html       # 主页面
│   ├── app.js          # 前端逻辑
│   └── styles.css      # 样式文件
├── data/                # 数据文件
│   └── keywords.json    # 关键词数据库
├── vercel.json          # Vercel配置
└── package.json         # 项目配置
```

## 浏览器支持

- Chrome 88+
- Firefox 85+
- Safari 14+
- Edge 88+

## 许可证

MIT License